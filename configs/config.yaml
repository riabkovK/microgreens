app:
  port: "8000"
  readTimeout: 10 * time.Second
  writeTimeout: 10 * time.Second
  enablePrintRoutes: false
  maxHeaderSize: 1 << 20

db:
  username: "postgres"
  host: "localhost"
  port: "5436"
  dbname: "postgres"
  sslmode: "disable"